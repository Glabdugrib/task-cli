# Golang Task CLI

This is a minimal setup to build and run a Go application locally with the Go toolchain.

---

## 🔧 Local Go Commands

### Build the Application
```bash
go build
```

### Run the Application
```bash
go run .
```

### Format the Code
```bash
gofmt -w .
```